---
description: "Execute a build-ready plan gate by gate under TDD (failing tests first, quality commands green to close each gate), replan on contact with reality, and finish with the code-review pipeline."
argument-hint: "[plan slug or path — defaults to the most recent build-ready plan]"
---

Invoke the amux:build skill on the plan directory: $ARGUMENTS
